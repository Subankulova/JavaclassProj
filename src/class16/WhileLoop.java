package class16;

public class WhileLoop {
    public static void main(String[] args) {
        int activeUsers = 0;
        activeUsers++;
        System.out.println("Welcome user number " + activeUsers);
        activeUsers++;
        System.out.println("Welcome user number " + activeUsers);
        activeUsers++;
        System.out.println("Welcome user number " + activeUsers);
        activeUsers++;
        System.out.println("Welcome user number " + activeUsers);
        activeUsers++;
        System.out.println("Welcome user number " + activeUsers);
        activeUsers++;
        System.out.println("Welcome user number " + activeUsers);
        activeUsers++;
        System.out.println("Welcome user number " + activeUsers);
        activeUsers++;
        System.out.println("Welcome user number " + activeUsers);
        activeUsers++;
        System.out.println("Welcome user number " + activeUsers);
        activeUsers++;
        System.out.println("Welcome user number " + activeUsers);

        //() -> boolean expression
        //if boolean expression is true then body will be executed
        //while (){
        //in the body you must have a boolean expression updater
        //otherwise you might have an infinite loop.
        // }


     int activeU = 0;
     while (activeU < 5){
         activeU++;
         System.out.println("Welcome user number " + activeU);

    }


    }
}
